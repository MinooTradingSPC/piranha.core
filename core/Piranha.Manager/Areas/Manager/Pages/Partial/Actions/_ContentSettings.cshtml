<button v-if="settingRegions.length > 0 || ((usePrimaryImage || useExcerpt) && (useBlocks || contentRegions.length > 0))" class="btn btn-primary btn-icon" data-bs-toggle="modal" data-bs-target="#contentSettings">
    <i class="fas fa-cog"></i>
</button>
